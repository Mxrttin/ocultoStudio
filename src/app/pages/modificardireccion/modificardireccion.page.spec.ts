import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModificardireccionPage } from './modificardireccion.page';

describe('ModificardireccionPage', () => {
  let component: ModificardireccionPage;
  let fixture: ComponentFixture<ModificardireccionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ModificardireccionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
